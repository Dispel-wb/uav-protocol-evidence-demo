# 全链路智能通信分析系统：差距审计与实现表

## 当前结论

现有 V0.3 是“解调后字节证据分析原型”，已经证明已知协议解析、混合字节流分流、未知重复结构发现和规则复测可以在同一界面闭环运行。它距离“连续链路智能分析”还有数据时间轴、模型训练评测和跨捕获记忆三项关键能力；距离“射频接收、降噪、波形解调”还缺完整的 IQ 样本链路。

不使用一个容易误导的总完成百分比。下面按可验收关卡衡量距离：每个关卡必须有输入、输出、指标和回归样本。

## 总体顺序

### A. 解调后链路智能分析

- [x] A1 已知协议帧同步、校验和字段解析
- [x] A2 已知协议混合字节流分流
- [x] A3 未知重复结构归族、长度与校验假设
- [x] A4 捕获级覆盖率、完整性、协议组成、切换点和建议
- [x] A5 带时间戳的连续捕获与滑动窗口分析
  - [x] 定义 `capture-chunk-v1`：时间戳、来源、字节、丢包标志
  - [x] 跨块保留半帧，计算吞吐率、间隔和丢块计数
  - [x] 检测协议出现和消失
  - [ ] 增加版本漂移和质量突变检测（归入 A7 的模型与阈值评测）
- [x] A6 协议状态机与因果链
  - [x] 从命令、应答、心跳中恢复会话状态
  - [x] 把异常关联到前置条件和后续结果
  - [x] 用人工标注的正常、缺少前置条件和孤立应答轨迹验证
  - [ ] 扩展真实设备轨迹数据集并统计状态转换准确率（归入 A7 评测）
- [ ] A7 数据驱动的协议敏感度模型
  - [x] 建立带来源、分组和局限声明的数据集清单与可复现实验脚本
  - [ ] 收集公开或授权的真实捕获，按设备、协议、版本隔离训练与测试
  - [x] 实现可解释基线：字节直方图、哈希二元组和长度特征的质心分类器
  - [x] 加入相似度阈值与类别间隔阈值，低置信度执行开集拒识
  - [ ] 比较逻辑回归、梯度提升和轻量序列模型
  - [x] 用已知和未知验证样本联合校准相似度与类别间隔阈值
- [x] A8 主动调试与增量学习框架
  - [x] 根据不确定字段推荐下一项设备操作和验收口径
  - [x] 对受控操作前后数据做组内稳定、组间变化分析
  - [x] 新知识先进入候选区，经人工确认后才允许加入训练集
  - [ ] 在真实设备上执行多轮主动实验并统计字段恢复效果

**A 阶段完成标准：** 对未参与训练的设备与捕获，输出协议候选、置信度、字段/状态证据和拒识结果；测试集必须按设备隔离，并报告准确率、未知召回率、误报率和分析耗时。

### B. 离线 IQ 信号接入与解调

- [x] B1 统一 IQ 文件入口
  - [x] 采用 SigMF 数据与元数据格式
  - [x] 支持 `cf32_le/be` 与 `ci16_le/be`
  - [x] 保存中心频率、采样率、设备和描述信息
  - [ ] 用真实SDR采样验证设备时间戳和增益元数据
- [x] B2 信号检测与质量估计
  - [x] 去直流、功率谱、频谱峰值、占用带宽与噪声估计
  - [x] 能量检测并切出突发区间
  - [x] 输出无参考SNR估计和检测阈值证据
  - [ ] 用校准信号或已知序列复核SNR偏差
- [ ] B3 最小降噪链
  - [x] 去直流
  - [x] 双音频谱峰值驱动的频偏粗校正
  - [x] 窗函数FIR低通和突发区间增益归一化
  - [x] 用固定已知比特样本计算处理后BER
  - [ ] 用多档真实SNR、频偏和干扰条件评估稳健性
- [x] B4 单一调制解调闭环
  - [x] 限定为已知符号率的2-FSK，不同时铺开全部制式
  - [x] 完成频偏校正、符号定时、硬判决和同步字搜索
  - [x] 恢复字节并送入A阶段，MAVLink帧CRC验证通过
  - [ ] 用真实设备IQ验证，而非只用合成样本
- [x] B5 多候选解调与选择（当前限定2-FSK）
  - [x] 对800/1000/1200/2400等少量候选符号率逐一尝试
  - [x] 以同步字误差、判决置信度和MAVLink CRC证据联合选路
  - [x] 保存失败候选及失败原因，避免黑箱选择
  - [ ] 扩展到GFSK、BPSK/QPSK等其他调制候选

**B 阶段完成标准：** 对固定录制的 IQ 数据，处理后能够稳定恢复字节；每次运行结果一致，并提供处理前后指标、参数和中间产物。

### C. 实时射频接收

- [ ] C1 设备无关采样接口
  - [x] 实现可选SoapySDR适配器，支持设备枚举和连续IQ读取接口
  - [x] 暴露中心频率、采样率、带宽和增益
  - [x] 检测溢出、超时、空读和时间戳跳变并写入报告
  - [ ] 安装具体SDR驱动并使用真实硬件验证
- [ ] C2 实时缓冲和回放
  - [x] 固定容量环形缓冲区与覆盖计数
  - [x] 同一采样接口既可读取实时源，也可落盘为SigMF后使用数组源回放
  - [ ] 射频线程与分析线程故障隔离
- [ ] C3 在线降噪、解调与协议反馈
  - [x] 将B阶段模块接入统一采样源，完成有限窗口的RF到协议闭环
  - [x] 用协议CRC反馈候选符号率选择
  - [x] 报告处理耗时、接收样本、超时、溢出和时间戳异常
  - [ ] 改为持续流式DSP，并加入状态一致性反馈和实时延迟分位数
  - [ ] 使用真实硬件和真实信号连续运行验证

**C 阶段完成标准：** 指定硬件和实验信号源下连续运行，实时字节输出与离线回放结果一致；断开设备、溢出和弱信号均能明确报错并留下证据。

## 为什么按这个顺序

1. A 阶段决定系统最终要从字节中证明什么，也是 B/C 阶段的验收器。
2. 先做离线 IQ，可以固定输入、重复实验，避免硬件、环境和算法问题同时变化。
3. 最后接实时硬件，SoapySDR 只负责设备与流；采样文件采用 SigMF，解调算法保持独立。

## 技术边界

- 未知协议结构推断不等于破解加密，也不能仅凭字节证明字段业务含义。
- 降噪不能恢复已被信道彻底抹去的信息，必须用 BER、EVM 或协议校验率评估。
- “自动识别调制方式”和“自动解调任意协议”不能作为单一功能验收，应限定信号集合、频段、采样率和信噪比。
- 模型训练数据必须来自公开数据或明确授权采集，训练集与测试集按设备隔离。

## 已选接口依据

- [SoapySDR](https://github.com/pothosware/SoapySDR)：设备枚举、采样参数和连续流接口。
- [SigMF](https://sigmf.org/)：IQ 样本及采样率、中心频率等元数据的可复现存储。
- [GNU Radio Symbol Sync](https://wiki.gnuradio.org/index.php/Symbol_Sync)：载波恢复、符号同步、滤波和解调模块的验证环境。
- Web Demo：继续承担字节层证据展示；RF/DSP 运行在本地后端，通过稳定报告格式连接。

## 协议推断方法依据

- [Discoverer](https://www.usenix.org/conference/16th-usenix-security-symposium/presentation/discoverer-automatic-protocol-reverse-enginee)：从消息轨迹进行保守聚类和格式推断，强调不要把不同格式错误合并。
- [NetPlier](https://www.ndss-symposium.org/ndss-paper/netplier-probabilistic-network-protocol-reverse-engineering-from-message-traces/)：通过多序列对齐和概率推断识别消息类型字段，并进一步恢复状态关系。
- [DynPRE](https://www.ndss-symposium.org/ndss-paper/dynpre-protocol-reverse-engineering-via-dynamic-inference/)：通过交互产生额外流量和语义证据，支持本项目的受控操作与主动调试方向。
