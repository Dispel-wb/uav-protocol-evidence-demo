# 实际操作演示素材

`demo_recording.mp4` 是在浏览器中操作真实 Demo 录制的视频，并非界面动画。`keyframes/` 中的 8 张 1920×1080 截图按演示顺序编号，可直接用于答辩 PPT：

1. 正常捕获识别 7 帧；
2. 起飞指令字段与原始字节；
3. 修改高度阈值并重跑；
4. 异常捕获形成 3 项证据；
5. SBUS 16 通道；
6. SBUS 失控保护；
7. 同一捕获内识别两类协议；
8. 导出证据报告。

录屏所用输入均为 `samples/` 中随仓库提供的字节流。

